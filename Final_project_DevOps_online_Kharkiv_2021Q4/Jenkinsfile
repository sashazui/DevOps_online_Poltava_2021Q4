pipeline {
    agent any

    stages {
        stage('all repository') {
            steps {
                sh "ls -la"
                echo "Building......."
                echo "End of Stage Build"
            }
        }
    }
}
